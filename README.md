## Shasha Revolt

Shasha Revolt Bot

## Usage
- Install all packages by running `npm i`
- Fill `.env` (see `example.env`)
- Run `npx sequelize --env production db:create && npx sequelize --env production db:migrate`
- Run the bot `npm run start`
